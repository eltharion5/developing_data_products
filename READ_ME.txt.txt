This shiny app is a predictor for the mpg variable from the mtcars dataset:

This predictor takes three parameters:

	qsec
 	wt
	am (automatic or manual)

The prediction is performed using a multiparameters linear regression

The result is displayed and represented on the plot.